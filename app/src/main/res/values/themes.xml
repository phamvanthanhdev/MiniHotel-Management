<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MiniHotelManagement" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:popupMenuStyle">@style/popupBGStyle</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MiniHotelManagement" parent="Base.Theme.MiniHotelManagement" />
    <style name="popupBGStyle">
        <item name="android:popupBackground">@drawable/menu_bg</item>
    </style>
</resources>